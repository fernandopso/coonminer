.row
  .col-xs-12.col-sm-5
    = render partial: 'tweets/filter', locals: { path: token_hashtags_path(params[:token_id]) }
    #chart_xs.hidden-sm.hidden-md.hidden-lg
    = render partial: 'hashtags', locals: { hashtags: @hashtags }

  .col-sm-7.hidden-xs
    = render partial: 'tokens/tweets/accuracy', locals: { token: @token, amount: @hashtags.count, model_name: Hashtag.model_name.human }
    #chart

= render partial: 'charts/pie', locals: { options: @chart_options }
