.text-center
  %b
    - if controller_name != 'sessions'
      = link_to "Sign in with your email", new_session_path(resource_name)
    - if devise_mapping.registerable? && controller_name != 'registrations'
      = link_to "Register with your email", new_registration_path(resource_name)
%br
